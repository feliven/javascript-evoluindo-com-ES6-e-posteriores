const ui = {
    limparFormulario() {
        const formulario = document.getElementById("pensamento-form");
        if (formulario) {
            formulario.reset();
        }
    },
};
export default ui;
//# sourceMappingURL=ui.js.map